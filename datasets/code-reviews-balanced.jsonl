{"input": "Review the following code change:\n\nFile: src/utils/validation.ts\n```typescript\ninterface ValidationResult {\n  valid: boolean;\n  errors: string[];\n}\n\nexport function validateEmail(email: string): ValidationResult {\n  const errors: string[] = [];\n\n  if (!email) {\n    errors.push('Email is required');\n  } else {\n    const emailRegex = /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/;\n    if (!emailRegex.test(email)) {\n      errors.push('Invalid email format');\n    }\n  }\n\n  return {\n    valid: errors.length === 0,\n    errors,\n  };\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\nNone\n\n### Suggestions\n1. **Consider email length validation** (src/utils/validation.ts)\n   - RFC 5321 limits email to 254 characters\n   - Add: `if (email.length > 254) errors.push('Email too long');`\n\n### Good Practices Noted\n- Clear return type interface\n- Comprehensive null/empty check\n- Standard regex pattern for basic validation\n- Immutable errors array pattern\n\n### Summary\n- 0 critical issues\n- 0 warnings\n- 1 suggestion for improvement", "metadata": {"severity": "suggestion", "category": "best_practices", "issue_type": "edge_cases", "language": "typescript", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: pkg/parser/reader.go\n```go\nfunc ReadData(r io.Reader, size int) ([]byte, error) {\n    buf := make([]byte, 1024)\n    n, err := r.Read(buf)\n    if err != nil {\n        return nil, err\n    }\n    return buf[:size], nil\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Buffer Over-read** (pkg/parser/reader.go:6)\n   - Severity: Critical\n   - Returns `buf[:size]` but `size` may exceed `n` (bytes actually read)\n   - Can expose uninitialized memory or panic\n\n   **Recommended fix**:\n   ```go\n   func ReadData(r io.Reader, size int) ([]byte, error) {\n       buf := make([]byte, size)\n       n, err := r.Read(buf)\n       if err != nil && err != io.EOF {\n           return nil, err\n       }\n       return buf[:n], nil\n   }\n   ```\n\n### Summary\n- 1 critical issue requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "memory", "issue_type": "buffer_overflow", "language": "go", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/services/payment.ts\n```typescript\nexport async function processPayment(order: Order) {\n  const response = await fetch('https://api.stripe.com/v1/charges', {\n    method: 'POST',\n    headers: {\n      'Authorization': `Bearer ${process.env.STRIPE_SECRET_KEY}`,\n      'Content-Type': 'application/x-www-form-urlencoded'\n    },\n    body: `amount=${order.total}&currency=usd&source=${order.paymentToken}`\n  });\n  \n  return response.json();\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\n1. **Use Official SDK** (src/services/payment.ts:2-9)\n   - Direct API calls are error-prone and miss SDK features\n   - Stripe SDK handles retries, errors, and edge cases\n   \n   **Recommended fix**:\n   ```typescript\n   import Stripe from 'stripe';\n   const stripe = new Stripe(process.env.STRIPE_SECRET_KEY!);\n   \n   export async function processPayment(order: Order) {\n     return stripe.charges.create({\n       amount: order.total,\n       currency: 'usd',\n       source: order.paymentToken\n     });\n   }\n   ```\n\n2. **Missing Error Handling** (src/services/payment.ts:11)\n   - No check for failed payment response\n   - No try/catch for network errors\n\n3. **No Idempotency Key** (src/services/payment.ts)\n   - Payment could be duplicated on retry\n   - Stripe SDK handles this automatically\n\n### Summary\n- 0 critical issues\n- 3 warnings to address before merge\n- 0 suggestions", "metadata": {"severity": "warning", "category": "best_practices", "issue_type": "use_sdk"}}
{"input": "Review the following code change:\n\nFile: pkg/api/handlers.go\n```go\nfunc CreateUser(w http.ResponseWriter, r *http.Request) {\n    var user User\n    json.NewDecoder(r.Body).Decode(&user)\n\n    db.Create(&user)\n\n    json.NewEncoder(w).Encode(user)\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\n1. **Ignored Error Returns** (pkg/api/handlers.go:3-5)\n   - Severity: Warning\n   - Decode, Create, and Encode errors are silently ignored\n   - Client receives incorrect response on failure\n\n   **Recommended fix**:\n   ```go\n   func CreateUser(w http.ResponseWriter, r *http.Request) {\n       var user User\n       if err := json.NewDecoder(r.Body).Decode(&user); err != nil {\n           http.Error(w, \"Invalid request body\", http.StatusBadRequest)\n           return\n       }\n\n       if err := db.Create(&user).Error; err != nil {\n           http.Error(w, \"Failed to create user\", http.StatusInternalServerError)\n           return\n       }\n\n       w.Header().Set(\"Content-Type\", \"application/json\")\n       if err := json.NewEncoder(w).Encode(user); err != nil {\n           log.Printf(\"Failed to encode response: %v\", err)\n       }\n   }\n   ```\n\n### Summary\n- 0 critical issues\n- 1 warning to address before merge\n- 0 suggestions", "metadata": {"severity": "warning", "category": "error_handling", "issue_type": "ignored_errors", "language": "go", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/services/authService.ts\n```typescript\nexport function validateToken(token: string): User | null {\n  const decoded = jwt.decode(token);\n  \n  if (!decoded) {\n    return null;\n  }\n  \n  return {\n    id: decoded.sub,\n    email: decoded.email,\n    role: decoded.role\n  };\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Missing Token Signature Verification** (src/services/authService.ts)\n   - Token is decoded but never verified using `jwt.verify()`\n   - Attacker can forge any token and bypass authentication\n   - This is a critical security vulnerability\n   \n   **Recommended fix**: Use `jwt.verify()` with secret key:\n   ```typescript\n   export function validateToken(token: string): User | null {\n     try {\n       const decoded = jwt.verify(token, process.env.JWT_SECRET);\n       \n       return {\n         id: decoded.sub,\n         email: decoded.email,\n         role: decoded.role\n       };\n     } catch (error) {\n       return null;\n     }\n   }\n   ```\n\n### Warnings\n1. **Missing Expiration Check** (src/services/authService.ts)\n   - Token expiration (`exp` claim) is not validated\n   - Expired tokens will be accepted as valid\n   \n   **Recommended fix**: `jwt.verify()` automatically validates expiration, or manually check: `if (decoded.exp && decoded.exp < Date.now() / 1000) return null;`", "metadata": {"source_demo_score": 1.0, "source": "synthetic_similar", "synthetic": true}}
{"input": "Review the following code change:\n\nFile: pkg/calc/size.go\n```go\nfunc CalculateTotalSize(count int32, itemSize int32) int32 {\n    return count * itemSize\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Integer Overflow** (pkg/calc/size.go:2)\n   - Severity: Critical\n   - CWE-190: Integer Overflow\n   - Multiplying two int32 values can overflow\n   - Could lead to buffer allocation bugs or incorrect calculations\n\n   **Recommended fix**:\n   ```go\n   func CalculateTotalSize(count int32, itemSize int32) (int64, error) {\n       result := int64(count) * int64(itemSize)\n       if result > math.MaxInt64 || result < 0 {\n           return 0, errors.New(\"size overflow\")\n       }\n       return result, nil\n   }\n   ```\n\n### Summary\n- 1 critical issue requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "numeric", "issue_type": "integer_overflow", "language": "go", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/services/orderService.ts\n```typescript\nimport { Order, OrderStatus } from '../types';\nimport { db } from '../db';\nimport { logger } from '../utils/logger';\n\nexport async function updateOrderStatus(\n  orderId: string,\n  newStatus: OrderStatus,\n  userId: string\n): Promise<Order> {\n  const order = await db.orders.findById(orderId);\n\n  if (!order) {\n    throw new NotFoundError(`Order ${orderId} not found`);\n  }\n\n  if (order.userId !== userId) {\n    throw new ForbiddenError('Cannot modify another user\\'s order');\n  }\n\n  const validTransitions: Record<OrderStatus, OrderStatus[]> = {\n    pending: ['confirmed', 'cancelled'],\n    confirmed: ['shipped', 'cancelled'],\n    shipped: ['delivered'],\n    delivered: [],\n    cancelled: [],\n  };\n\n  if (!validTransitions[order.status].includes(newStatus)) {\n    throw new ValidationError(\n      `Cannot transition from ${order.status} to ${newStatus}`\n    );\n  }\n\n  const updated = await db.orders.update(orderId, { status: newStatus });\n\n  logger.info('Order status updated', { orderId, oldStatus: order.status, newStatus });\n\n  return updated;\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\nNone\n\n### Suggestions\nNone\n\n### Good Practices Noted\n- Proper authorization check (user owns order)\n- State machine pattern for valid transitions\n- Clear error messages with context\n- Audit logging for status changes\n- Type-safe status enum\n- Single responsibility principle\n\n### Summary\n- 0 critical issues\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "none", "category": "best_practices", "issue_type": "clean_code", "language": "typescript", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/utils/helpers.ts\n```typescript\nexport function calculateDiscount(price: number, discount: number): number {\n  return price - (price * discount / 100);\n}\n\nexport function formatDate(date: Date): string {\n  return date.toISOString().split('T')[0];\n}\n\nexport function capitalize(str: string): string {\n  return str.charAt(0).toUpperCase() + str.slice(1);\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\nNone\n\n### Suggestions\n1. **Consider edge case handling** (src/utils/helpers.ts:2)\n   - `calculateDiscount` could validate inputs:\n   ```typescript\n   if (price < 0 || discount < 0 || discount > 100) {\n     throw new Error('Invalid price or discount');\n   }\n   ```\n\n2. **Handle empty string** (src/utils/helpers.ts:10)\n   - `capitalize('')` returns '' but could be explicit:\n   ```typescript\n   if (!str) return str;\n   ```\n\n### Good Practices Noted\n- Functions have single responsibility\n- Clear naming conventions\n- Simple, readable implementations\n\n### Summary\n- 0 critical issues\n- 0 warnings\n- 2 suggestions for improvement", "metadata": {"severity": "suggestion", "category": "code_quality", "issue_type": "edge_cases"}}
{"input": "Review the following code change:\n\nFile: pkg/handlers/api.go\n```go\nfunc GetUserID(ctx context.Context) int64 {\n    userID := ctx.Value(\"user_id\").(int64)\n    return userID\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Unchecked Type Assertion** (pkg/handlers/api.go:2)\n   - Severity: Critical\n   - Will panic if key missing or type mismatch\n   - Production service could crash on bad request\n\n   **Recommended fix**:\n   ```go\n   func GetUserID(ctx context.Context) (int64, error) {\n       userID, ok := ctx.Value(\"user_id\").(int64)\n       if !ok {\n           return 0, errors.New(\"user_id not found in context\")\n       }\n       return userID, nil\n   }\n   ```\n\n### Summary\n- 1 critical issue requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "runtime", "issue_type": "unchecked_assertion", "language": "go", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: pkg/handlers/timeout.go\n```go\nfunc FetchWithTimeout(url string) ([]byte, error) {\n    client := &http.Client{}\n    resp, err := client.Get(url)\n    if err != nil {\n        return nil, err\n    }\n    defer resp.Body.Close()\n    return ioutil.ReadAll(resp.Body)\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\n1. **Missing Request Timeout** (pkg/handlers/timeout.go:2)\n   - Severity: Warning\n   - HTTP client has no timeout configured\n   - Slow or hanging servers will block goroutine indefinitely\n   - Could lead to resource exhaustion\n\n   **Recommended fix**:\n   ```go\n   func FetchWithTimeout(url string) ([]byte, error) {\n       client := &http.Client{\n           Timeout: 30 * time.Second,\n       }\n       resp, err := client.Get(url)\n       if err != nil {\n           return nil, err\n       }\n       defer resp.Body.Close()\n\n       // Also limit body read\n       return io.ReadAll(io.LimitReader(resp.Body, 10*1024*1024)) // 10MB max\n   }\n   ```\n\n### Summary\n- 0 critical issues\n- 1 warning to address before merge\n- 0 suggestions", "metadata": {"severity": "warning", "category": "reliability", "issue_type": "missing_timeout", "language": "go", "synthetic": true, "source": "phase5_synthetic_extra"}}
{"input": "Review the following code change:\n\nFile: src/main/java/com/app/UserService.java\n```java\npublic String getUserDisplayName(Long userId) {\n    User user = userRepository.findById(userId);\n    return user.getFirstName() + \" \" + user.getLastName();\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Null Pointer Dereference** (src/main/java/com/app/UserService.java:3)\n   - Severity: Critical\n   - CWE-476: NULL Pointer Dereference\n   - user may be null if not found, causing NullPointerException\n\n   **Recommended fix**:\n   ```java\n   public String getUserDisplayName(Long userId) {\n       User user = userRepository.findById(userId);\n       if (user == null) {\n           throw new UserNotFoundException(\"User not found: \" + userId);\n       }\n       return user.getFirstName() + \" \" + user.getLastName();\n   }\n   ```\n\n   Or using Optional:\n   ```java\n   public String getUserDisplayName(Long userId) {\n       return userRepository.findById(userId)\n           .map(u -> u.getFirstName() + \" \" + u.getLastName())\n           .orElseThrow(() -> new UserNotFoundException(\"User not found: \" + userId));\n   }\n   ```\n\n### Summary\n- 1 critical issue requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "null_safety", "issue_type": "null_dereference", "language": "java", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/middleware/rateLimit.ts\n```typescript\nconst requestCounts = new Map<string, number>();\n\nexport function rateLimit(req: Request, res: Response, next: Function) {\n  const ip = req.ip;\n  const count = requestCounts.get(ip) || 0;\n  \n  if (count >= 100) {\n    res.status(429).json({ error: 'Too many requests' });\n    return;\n  }\n  \n  requestCounts.set(ip, count + 1);\n  next();\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\n1. **Rate Limit Never Resets** (src/middleware/rateLimit.ts)\n   - Request counts only increment, never reset\n   - Once 100 requests reached, IP is permanently blocked\n   \n   **Recommended fix**: Add time window:\n   ```typescript\n   const requests = new Map<string, { count: number; resetAt: number }>();\n   const WINDOW_MS = 60 * 1000; // 1 minute\n   const MAX_REQUESTS = 100;\n   \n   export function rateLimit(req: Request, res: Response, next: Function) {\n     const ip = req.ip;\n     const now = Date.now();\n     const record = requests.get(ip);\n     \n     if (!record || now > record.resetAt) {\n       requests.set(ip, { count: 1, resetAt: now + WINDOW_MS });\n       next();\n       return;\n     }\n     \n     if (record.count >= MAX_REQUESTS) {\n       res.status(429).json({ error: 'Too many requests' });\n       return;\n     }\n     \n     record.count++;\n     next();\n   }\n   ```\n\n### Summary\n- 0 critical issues\n- 1 warning to address before merge\n- 0 suggestions", "metadata": {"severity": "warning", "category": "logic", "issue_type": "incomplete_implementation"}}
{"input": "Review the following code change:\n\nFile: src/config/database.ts\n```typescript\nimport { Pool } from 'pg';\n\nexport const pool = new Pool({\n  host: process.env.DB_HOST,\n  port: parseInt(process.env.DB_PORT || '5432'),\n  database: process.env.DB_NAME,\n  user: process.env.DB_USER,\n  password: process.env.DB_PASSWORD,\n  max: 20,\n  idleTimeoutMillis: 30000,\n  connectionTimeoutMillis: 2000\n});\n\npool.on('error', (err) => {\n  console.error('Unexpected database error', err);\n});\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\nNone\n\n### Suggestions\n1. **Consider SSL in production** (src/config/database.ts)\n   - Add SSL configuration for production:\n   ```typescript\n   ssl: process.env.NODE_ENV === 'production' ? { rejectUnauthorized: true } : false\n   ```\n\n2. **Use connection string option** (src/config/database.ts)\n   - Simpler to configure via DATABASE_URL:\n   ```typescript\n   export const pool = new Pool({\n     connectionString: process.env.DATABASE_URL,\n     max: 20\n   });\n   ```\n\n### Good Practices Noted\n- Environment variables used for credentials\n- Connection pooling configured\n- Error handler registered\n- Reasonable timeout values\n\n### Summary\n- 0 critical issues\n- 0 warnings\n- 2 suggestions for improvement", "metadata": {"severity": "suggestion", "category": "best_practices", "issue_type": "configuration"}}
{"input": "Review the following code change:\n\nFile: src/services/counter.py\n```python\nimport threading\n\ncounter = 0\n\ndef increment():\n    global counter\n    temp = counter\n    temp += 1\n    counter = temp\n\ndef get_count():\n    return counter\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Race Condition** (src/services/counter.py:6-9)\n   - Severity: Critical\n   - Non-atomic read-modify-write on shared state\n   - Multiple threads can read same value before writing\n\n   **Recommended fix**:\n   ```python\n   import threading\n\n   counter = 0\n   counter_lock = threading.Lock()\n\n   def increment():\n       global counter\n       with counter_lock:\n           counter += 1\n   ```\n\n### Summary\n- 1 critical issue requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "concurrency", "issue_type": "race_condition", "language": "python", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/main/java/com/app/DatabaseConfig.java\n```java\npublic class DatabaseConfig {\n    private static final String DB_URL = \"jdbc:mysql://prod-db.example.com:3306/app\";\n    private static final String DB_USER = \"admin\";\n    private static final String DB_PASSWORD = \"P@ssw0rd123!\";\n\n    public Connection getConnection() throws SQLException {\n        return DriverManager.getConnection(DB_URL, DB_USER, DB_PASSWORD);\n    }\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Hardcoded Credentials** (src/main/java/com/app/DatabaseConfig.java:3-4)\n   - Severity: Critical\n   - CWE-798: Use of Hard-coded Credentials\n   - Credentials exposed in source code and version control\n\n   **Recommended fix**:\n   ```java\n   public class DatabaseConfig {\n       public Connection getConnection() throws SQLException {\n           String dbUrl = System.getenv(\"DB_URL\");\n           String dbUser = System.getenv(\"DB_USER\");\n           String dbPassword = System.getenv(\"DB_PASSWORD\");\n\n           if (dbUrl == null || dbUser == null || dbPassword == null) {\n               throw new IllegalStateException(\"Database credentials not configured\");\n           }\n\n           return DriverManager.getConnection(dbUrl, dbUser, dbPassword);\n       }\n   }\n   ```\n\n2. **Production URL in Code** (src/main/java/com/app/DatabaseConfig.java:2)\n   - Severity: High\n   - Production server address exposed\n   - Should use environment-specific configuration\n\n### Summary\n- 2 critical issues requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "security", "issue_type": "hardcoded_credentials", "language": "java", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/utils/formatter.py\n```python\ndef format_report(items: list[dict]) -> str:\n    result = \"\"\n    for item in items:\n        result += f\"Name: {item['name']}\\n\"\n        result += f\"Value: {item['value']}\\n\"\n        result += \"---\\n\"\n    return result\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\n1. **Inefficient String Concatenation** (src/utils/formatter.py:3-6)\n   - Severity: Warning\n   - String concatenation in loop creates O(n\u00b2) performance\n   - Each += creates a new string object\n\n   **Recommended fix**:\n   ```python\n   def format_report(items: list[dict]) -> str:\n       lines = []\n       for item in items:\n           lines.append(f\"Name: {item['name']}\")\n           lines.append(f\"Value: {item['value']}\")\n           lines.append(\"---\")\n       return \"\\n\".join(lines)\n   ```\n\n### Summary\n- 0 critical issues\n- 1 warning to address before merge\n- 0 suggestions", "metadata": {"severity": "warning", "category": "performance", "issue_type": "string_concat", "language": "python", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/main/java/com/app/Cache.java\n```java\npublic class Cache {\n    private Map<String, Object> store = new HashMap<>();\n\n    public void put(String key, Object value) {\n        store.put(key, value);\n    }\n\n    public Object get(String key) {\n        return store.get(key);\n    }\n\n    public void clear() {\n        store.clear();\n    }\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Thread Safety Violation** (src/main/java/com/app/Cache.java:2-12)\n   - Severity: Critical\n   - HashMap is not thread-safe\n   - Concurrent access can cause infinite loops, data corruption, or lost updates\n\n   **Recommended fix**:\n   ```java\n   public class Cache {\n       private final ConcurrentHashMap<String, Object> store = new ConcurrentHashMap<>();\n\n       public void put(String key, Object value) {\n           store.put(key, value);\n       }\n\n       public Object get(String key) {\n           return store.get(key);\n       }\n\n       public void clear() {\n           store.clear();\n       }\n   }\n   ```\n\n### Summary\n- 1 critical issue requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "concurrency", "issue_type": "thread_safety", "language": "java", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/services/http_client.py\n```python\nimport aiohttp\n\nasync def fetch_data(url: str) -> dict:\n    session = aiohttp.ClientSession()\n    response = await session.get(url)\n    data = await response.json()\n    return data\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Resource Leak** (src/services/http_client.py:4)\n   - Severity: Critical\n   - aiohttp.ClientSession never closed\n   - Will leak TCP connections and cause \"too many open files\" errors\n\n   **Recommended fix**:\n   ```python\n   import aiohttp\n\n   async def fetch_data(url: str) -> dict:\n       async with aiohttp.ClientSession() as session:\n           async with session.get(url) as response:\n               return await response.json()\n   ```\n\n   Or for reusable sessions:\n   ```python\n   # Create session once at app startup\n   session = aiohttp.ClientSession()\n\n   async def fetch_data(url: str) -> dict:\n       async with session.get(url) as response:\n           return await response.json()\n\n   # Close at app shutdown\n   async def cleanup():\n       await session.close()\n   ```\n\n### Summary\n- 1 critical issue requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "resource", "issue_type": "async_leak", "language": "python", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/services/auth.ts\n```typescript\nimport crypto from 'crypto';\n\nexport function validateToken(token: string) {\n  const secret = process.env.JWT_SECRET;\n  const decoded = crypto.createHmac('sha256', secret).update(token).digest('hex');\n  \n  if (decoded === token) {\n    return { valid: true, authenticated: true };\n  }\n  return { valid: false };\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Timing Attack Vulnerability** (src/services/auth.ts:6)\n   - Severity: Critical\n   - String comparison using `===` is vulnerable to timing attacks\n   - Attacker can derive token by measuring response times\n   \n   **Recommended fix**:\n   ```typescript\n   import crypto from 'crypto';\n   \n   export function validateToken(token: string) {\n     const secret = process.env.JWT_SECRET;\n     const decoded = crypto.createHmac('sha256', secret).update(token).digest('hex');\n     \n     // Use constant-time comparison\n     const isValid = crypto.timingSafeEqual(\n       Buffer.from(decoded),\n       Buffer.from(token)\n     ).catch(() => false);\n     \n     if (isValid) {\n       return { valid: true, authenticated: true };\n     }\n     return { valid: false };\n   }\n   ```\n\n2. **Missing Input Validation** (src/services/auth.ts:4)\n   - Severity: High\n   - No check for empty or undefined token\n   - No validation of token format before processing\n   \n   **Recommended fix**:\n   ```typescript\n   if (!token || typeof token !== 'string' || token.length === 0) {\n     return { valid: false };\n   }\n   ```\n\n3. **Unhandled Exception** (src/services/auth.ts:5)\n   - Severity: High\n   - Missing error handling for undefined JWT_SECRET\n   - Will crash if environment variable not set\n   \n   **Recommended fix**:\n   ```typescript\n   const secret = process.env.JWT_SECRET;\n   if (!secret) {\n     throw new Error('JWT_SECRET environment variable not configured');\n   }\n   ```", "metadata": {"source_demo_score": 1.0, "source": "synthetic_similar", "synthetic": true}}
{"input": "Review the following code change:\n\nFile: src/hooks/useAuth.ts\n```typescript\nimport { useState, useEffect } from 'react';\n\nexport function useAuth() {\n  const [user, setUser] = useState(null);\n  \n  useEffect(() => {\n    const token = localStorage.getItem('token');\n    if (token) {\n      fetch('/api/me', {\n        headers: { Authorization: `Bearer ${token}` }\n      })\n        .then(res => res.json())\n        .then(data => setUser(data));\n    }\n  }, []);\n  \n  return { user };\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\n1. **Missing Error Handling** (src/hooks/useAuth.ts:9-12)\n   - No error handling for failed API call\n   - No handling for non-200 responses\n   \n   **Recommended fix**:\n   ```typescript\n   useEffect(() => {\n     const token = localStorage.getItem('token');\n     if (!token) return;\n     \n     fetch('/api/me', {\n       headers: { Authorization: `Bearer ${token}` }\n     })\n       .then(res => {\n         if (!res.ok) throw new Error('Failed to fetch user');\n         return res.json();\n       })\n       .then(data => setUser(data))\n       .catch(err => {\n         console.error('Auth error:', err);\n         localStorage.removeItem('token');\n       });\n   }, []);\n   ```\n\n### Suggestions\n1. **Consider using async/await** (src/hooks/useAuth.ts)\n   - Would improve readability\n\n2. **Add loading state** (src/hooks/useAuth.ts)\n   - UI can show loading indicator while fetching\n\n### Summary\n- 0 critical issues\n- 1 warning to address before merge\n- 2 suggestions for improvement", "metadata": {"severity": "warning", "category": "error_handling", "issue_type": "missing_error_handling"}}
{"input": "Review the following code change:\n\nFile: src/utils/format.ts\n```typescript\nfunction formatCurrency(amount: number): string {\n  if (amount < 0) {\n    return '-$' + Math.abs(amount).toFixed(2);\n  }\n  return '$' + amount.toFixed(2);\n}\n\nfunction formatPrice(price: number): string {\n  if (price < 0) {\n    return '-$' + Math.abs(price).toFixed(2);\n  }\n  return '$' + price.toFixed(2);\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\n1. **DRY Violation** (src/utils/format.ts:1-11)\n   - `formatCurrency` and `formatPrice` are identical functions\n   - Duplicate code increases maintenance burden\n   \n   **Suggested fix**: Remove `formatPrice` and use `formatCurrency` or extract shared logic:\n   ```typescript\n   function formatCurrency(amount: number): string {\n     const prefix = amount < 0 ? '-$' : '$';\n     return prefix + Math.abs(amount).toFixed(2);\n   }\n   // Alias if needed for semantic clarity\n   const formatPrice = formatCurrency;\n   ```\n\n### Summary\n- 0 critical issues\n- 1 warning to address before merge\n- 0 suggestions", "metadata": {"severity": "warning", "category": "code_quality", "issue_type": "dry_violation"}}
{"input": "Review the following code change:\n\nFile: src/components/UserCard.tsx\n```typescript\ninterface User {\n  name: string;\n  email: string;\n  profile?: { avatar: string };\n}\n\nfunction UserCard({ user }: { user: User }) {\n  return (\n    <div>\n      <img src={user.profile.avatar} alt={user.name} />\n      <h2>{user.name}</h2>\n    </div>\n  );\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\n1. **Potential Null Reference** (src/components/UserCard.tsx:9)\n   - Severity: Warning\n   - `user.profile` is optional but accessed without null check\n   - Will throw TypeError if profile is undefined\n\n   **Recommended fix**:\n   ```typescript\n   function UserCard({ user }: { user: User }) {\n     return (\n       <div>\n         {user.profile?.avatar && (\n           <img src={user.profile.avatar} alt={user.name} />\n         )}\n         <h2>{user.name}</h2>\n       </div>\n     );\n   }\n   ```\n\n### Summary\n- 0 critical issues\n- 1 warning to address before merge\n- 0 suggestions", "metadata": {"severity": "warning", "category": "null_safety", "issue_type": "null_reference", "language": "typescript", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/auth/login.ts\n```typescript\nasync function login(username: string, password: string) {\n  const user = await db.findUser(username);\n  if (!user) {\n    throw new Error('User not found: ' + username);\n  }\n  if (user.password !== password) {\n    throw new Error('Invalid password for user: ' + username);\n  }\n  return generateToken(user);\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Plain Text Password Comparison** (src/auth/login.ts:6)\n   - Severity: Critical\n   - Passwords should be hashed, not stored or compared as plain text\n   \n   **Recommended fix**:\n   ```typescript\n   const passwordMatch = await bcrypt.compare(password, user.passwordHash);\n   if (!passwordMatch) {\n     throw new Error('Invalid credentials');\n   }\n   ```\n\n2. **Information Disclosure in Error Messages** (src/auth/login.ts:4,6)\n   - Severity: Critical\n   - Error messages reveal whether username exists and are specific about failure type\n   - This enables username enumeration attacks\n   \n   **Recommended fix**: Use generic error message:\n   ```typescript\n   throw new Error('Invalid credentials');\n   ```\n\n### Summary\n- 2 critical issues requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "security", "issue_type": "authentication"}}
{"input": "Review the following code change:\n\nFile: src/api/users.ts\n```typescript\nasync function getUserById(id: string) {\n  const query = `SELECT * FROM users WHERE id = '${id}'`;\n  const result = await db.query(query);\n  return result.rows[0];\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **SQL Injection Vulnerability** (src/api/users.ts:2)\n   - Severity: Critical\n   - The user ID is directly interpolated into the SQL query\n   - Attacker can execute arbitrary SQL commands\n   \n   **Current (vulnerable)**:\n   ```typescript\n   const query = `SELECT * FROM users WHERE id = '${id}'`;\n   ```\n   \n   **Recommended fix**:\n   ```typescript\n   const query = 'SELECT * FROM users WHERE id = $1';\n   const result = await db.query(query, [id]);\n   ```\n\n### Summary\n- 1 critical issue requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "security", "issue_type": "sql_injection"}}
{"input": "Review the following code change:\n\nFile: src/lib/cache.ts\n```typescript\nconst cache = new Map();\n\nexport function getFromCache(key: string) {\n  return cache.get(key);\n}\n\nexport function setInCache(key: string, value: any, ttlMs: number) {\n  cache.set(key, value);\n  setTimeout(() => cache.delete(key), ttlMs);\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\n1. **Potential Memory Leak** (src/lib/cache.ts:8)\n   - If the same key is set multiple times, old timeouts are not cleared\n   - This can accumulate orphaned timeouts\n   \n   **Recommended fix**:\n   ```typescript\n   const cache = new Map();\n   const timeouts = new Map();\n   \n   export function setInCache(key: string, value: any, ttlMs: number) {\n     // Clear existing timeout for this key\n     const existingTimeout = timeouts.get(key);\n     if (existingTimeout) clearTimeout(existingTimeout);\n     \n     cache.set(key, value);\n     const timeout = setTimeout(() => {\n       cache.delete(key);\n       timeouts.delete(key);\n     }, ttlMs);\n     timeouts.set(key, timeout);\n   }\n   ```\n\n### Suggestions\n1. **Use typed generics** (src/lib/cache.ts:7)\n   - `value: any` loses type safety\n   \n   ```typescript\n   export function setInCache<T>(key: string, value: T, ttlMs: number): void\n   ```\n\n### Summary\n- 0 critical issues\n- 1 warning to address before merge\n- 1 suggestion for improvement", "metadata": {"severity": "warning", "category": "memory", "issue_type": "memory_leak"}}
{"input": "Review the following code change:\n\nFile: src/middleware/auth.ts\n```typescript\nfunction validateToken(token: string) {\n  const payload = JSON.parse(Buffer.from(token.split('.')[1], 'base64').toString());\n  if (payload.exp > Date.now()) {\n    return payload;\n  }\n  throw new Error('Token expired');\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Missing Token Signature Verification** (src/middleware/auth.ts:2)\n   - Severity: Critical\n   - Token payload is decoded but signature is never verified\n   - Attacker can forge any token with arbitrary claims\n   \n   **Current (vulnerable)**:\n   ```typescript\n   const payload = JSON.parse(Buffer.from(token.split('.')[1], 'base64').toString());\n   if (payload.exp > Date.now()) {\n     return payload;\n   }\n   ```\n   \n   **Recommended fix**:\n   ```typescript\n   const payload = jwt.verify(token, process.env.JWT_SECRET);\n   if (payload.exp > Date.now()) {\n     return payload;\n   }\n   ```\n\n2. **Incorrect Expiration Check** (src/middleware/auth.ts:3)\n   - Severity: High\n   - Comparing milliseconds to seconds: `payload.exp` is typically in seconds, `Date.now()` is in milliseconds\n   - Tokens will always appear expired or never expire\n   \n   **Recommended fix**:\n   ```typescript\n   if (payload.exp > Math.floor(Date.now() / 1000)) {\n     return payload;\n   }\n   ```", "metadata": {"source_demo_score": 1.0, "source": "synthetic_similar", "synthetic": true}}
{"input": "Review the following code change:\n\nFile: src/api/auth.ts\n```typescript\napp.get('/redirect', (req, res) => {\n  const returnUrl = req.query.return_url as string;\n  res.redirect(returnUrl);\n});\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Open Redirect Vulnerability** (src/api/auth.ts:2-3)\n   - Severity: Critical\n   - CWE-601: URL Redirection to Untrusted Site\n   - Attacker can redirect users to malicious sites\n\n   **Recommended fix**:\n   ```typescript\n   const ALLOWED_DOMAINS = ['example.com', 'app.example.com'];\n\n   app.get('/redirect', (req, res) => {\n     const returnUrl = req.query.return_url as string;\n\n     try {\n       const url = new URL(returnUrl);\n       if (!ALLOWED_DOMAINS.includes(url.hostname)) {\n         return res.status(400).send('Invalid redirect URL');\n       }\n       res.redirect(returnUrl);\n     } catch {\n       // Invalid URL format - redirect to home\n       res.redirect('/');\n     }\n   });\n   ```\n\n### Summary\n- 1 critical issue requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "security", "issue_type": "open_redirect", "language": "typescript", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "File: src/api/users.ts with `id = \"'; DROP TABLE users; --\"`", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **SQL Injection Vulnerability** (src/api/users.ts:2)\n   - Severity: Critical\n   - Malicious SQL injection: `'; DROP TABLE users; --` would execute destructive commands\n   - Query becomes: `SELECT * FROM users WHERE id = ''; DROP TABLE users; --'`", "metadata": {"edge_case": true, "source": "edge_case", "synthetic": true}}
{"input": "Review the following code change:\n\nFile: src/services/userService.ts\n```typescript\nasync function getUsersWithOrders() {\n  const users = await db.query('SELECT * FROM users');\n\n  for (const user of users) {\n    user.orders = await db.query(\n      'SELECT * FROM orders WHERE user_id = ?',\n      [user.id]\n    );\n  }\n\n  return users;\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\nNone\n\n### Warnings\n1. **N+1 Query Problem** (src/services/userService.ts:4-8)\n   - Severity: Warning\n   - Executes N+1 database queries (1 for users + 1 per user for orders)\n   - Performance degrades linearly with user count\n\n   **Recommended fix**:\n   ```typescript\n   async function getUsersWithOrders() {\n     const users = await db.query('SELECT * FROM users');\n     const userIds = users.map(u => u.id);\n\n     const orders = await db.query(\n       'SELECT * FROM orders WHERE user_id IN (?)',\n       [userIds]\n     );\n\n     const ordersByUser = orders.reduce((acc, order) => {\n       acc[order.user_id] = acc[order.user_id] || [];\n       acc[order.user_id].push(order);\n       return acc;\n     }, {});\n\n     return users.map(u => ({ ...u, orders: ordersByUser[u.id] || [] }));\n   }\n   ```\n\n### Summary\n- 0 critical issues\n- 1 warning to address before merge\n- 0 suggestions", "metadata": {"severity": "warning", "category": "performance", "issue_type": "n_plus_one", "language": "typescript", "synthetic": true, "source": "phase5_synthetic"}}
{"input": "Review the following code change:\n\nFile: src/main/java/com/app/FileService.java\n```java\npublic class FileService {\n    public String readFile(String path) throws IOException {\n        FileInputStream fis = new FileInputStream(path);\n        BufferedReader reader = new BufferedReader(new InputStreamReader(fis));\n        StringBuilder content = new StringBuilder();\n        String line;\n        while ((line = reader.readLine()) != null) {\n            content.append(line);\n        }\n        return content.toString();\n    }\n}\n```", "expected": "## Code Review Summary\n\n### Critical Issues\n1. **Resource Leak** (src/main/java/com/app/FileService.java:3-4)\n   - Severity: Critical\n   - FileInputStream and BufferedReader never closed\n   - File handles will leak, eventually exhausting system resources\n\n   **Recommended fix**:\n   ```java\n   public String readFile(String path) throws IOException {\n       try (FileInputStream fis = new FileInputStream(path);\n            BufferedReader reader = new BufferedReader(new InputStreamReader(fis))) {\n           StringBuilder content = new StringBuilder();\n           String line;\n           while ((line = reader.readLine()) != null) {\n               content.append(line);\n           }\n           return content.toString();\n       }\n   }\n   ```\n\n### Summary\n- 1 critical issue requiring immediate fix\n- 0 warnings\n- 0 suggestions", "metadata": {"severity": "critical", "category": "resource", "issue_type": "resource_leak", "language": "java", "synthetic": true, "source": "phase5_synthetic"}}
